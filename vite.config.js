import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset paths make the build portable for GitHub Pages
  // and for manual static hosting from any folder.
  base: './',
});
